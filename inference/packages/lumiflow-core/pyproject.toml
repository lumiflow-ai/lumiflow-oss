[project]
name = "lumiflow-core"
version = "0.1.0"
description = "Core library for Lumiflow"
readme = "README.md"
authors = []
requires-python = ">=3.11"
dependencies = [
    "colorama>=0.4.6",
    "pytest>=8.4.1",
    "requests>=2.32.4",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
