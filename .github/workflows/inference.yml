name: Inference

on:
  pull_request:
    branches:
      - main
    paths:
      - inference/**
      - .github/workflows/inference.yml
  push:
    branches:
      - main
    paths:
      - inference/**
      - .github/workflows/inference.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: ${{ github.ref_name != 'main' }}

permissions:
  contents: read

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Run inference lint
        uses: ./inference/.github/actions/lint

  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Run inference tests
        uses: ./inference/.github/actions/test
